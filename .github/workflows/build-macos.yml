name: Build macOS Application

on:
  workflow_dispatch:  # MANUAL TRIGGER ONLY - You click a button, no auto-runs
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
        default: '1.0.0'
      create_release:
        description: 'Create GitHub Release?'
        required: true
        type: boolean
        default: true

permissions:
  contents: write  # Required to create releases and upload assets

jobs:
  build-macos-intel:
    # Build Intel-only (x86_64) binary
    runs-on: macos-15-large  # Intel runner
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        echo "📦 Installing pip packages for Intel build..."
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        echo "✅ Dependencies installed"
    
    - name: Create macOS icon (.icns)
      run: |
        echo "🎨 Creating .icns icon for macOS..."
        mkdir -p app/icon.iconset
        sips -z 16 16 app/ICON.png --out app/icon.iconset/icon_16x16.png
        sips -z 32 32 app/ICON.png --out app/icon.iconset/icon_16x16@2x.png
        sips -z 32 32 app/ICON.png --out app/icon.iconset/icon_32x32.png
        sips -z 64 64 app/ICON.png --out app/icon.iconset/icon_32x32@2x.png
        sips -z 128 128 app/ICON.png --out app/icon.iconset/icon_128x128.png
        sips -z 256 256 app/ICON.png --out app/icon.iconset/icon_128x128@2x.png
        sips -z 256 256 app/ICON.png --out app/icon.iconset/icon_256x256.png
        sips -z 512 512 app/ICON.png --out app/icon.iconset/icon_256x256@2x.png
        sips -z 512 512 app/ICON.png --out app/icon.iconset/icon_512x512.png
        sips -z 1024 1024 app/ICON.png --out app/icon.iconset/icon_512x512@2x.png
        iconutil -c icns app/icon.iconset -o app/icon.icns
        rm -rf app/icon.iconset
        echo "✅ Icon created: app/icon.icns"
    
    - name: Build macOS Intel app
      run: |
        export MACOSX_DEPLOYMENT_TARGET=11.0
        
        echo "🔨 Building Intel (x86_64) app with PyInstaller..."
        echo "📋 Deployment target: macOS 11.0 Big Sur and later"
        python -m PyInstaller --name="ArgoLogViewer" \
          --windowed \
          --onefile \
          --icon="app/icon.icns" \
          --add-data="app:app" \
          --hidden-import=PySide6 \
          --hidden-import=paramiko \
          --hidden-import=cryptography \
          --clean \
          --osx-bundle-identifier=com.harshmeetsingh.argologviewer \
          --target-arch x86_64 \
          --codesign-identity - \
          app/main.py
    
    - name: Verify Intel binary
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🔍 VERIFYING INTEL BINARY"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        
        EXECUTABLE="dist/ArgoLogViewer.app/Contents/MacOS/ArgoLogViewer"
        
        if [ ! -f "$EXECUTABLE" ]; then
          echo "❌ ERROR: Executable not found at $EXECUTABLE"
          exit 1
        fi
        
        echo "📦 Checking architecture in executable:"
        lipo -info "$EXECUTABLE"
        file "$EXECUTABLE"
        echo ""
        
        if lipo -info "$EXECUTABLE" | grep -q "x86_64"; then
          echo "✅ SUCCESS: Binary is Intel x86_64!"
          echo "   ✓ Optimized for Intel Macs"
          echo "   ✓ Compatible with macOS 11.0+ (Big Sur, Monterey, Ventura, Sonoma)"
        else
          echo "❌ ERROR: Binary is not x86_64!"
          exit 1
        fi
        
        echo ""
        echo "📋 Executable info:"
        ls -lh "$EXECUTABLE"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    - name: Create Intel DMG installer
      run: |
        mkdir -p dist/dmg
        cp -r dist/ArgoLogViewer.app dist/dmg/
        ln -s /Applications dist/dmg/Applications
        hdiutil create -volname "Argo Log Viewer (Intel)" \
          -srcfolder dist/dmg \
          -ov -format UDZO \
          "ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.dmg"
        rm -rf dist/dmg
    
    - name: Zip the Intel .app
      run: |
        cd dist
        zip -r ../ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.zip ArgoLogViewer.app
        cd ..
    
    - name: Generate Intel checksums
      run: |
        shasum -a 256 ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.dmg > checksums-intel.txt
        shasum -a 256 ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.zip >> checksums-intel.txt
        cat checksums-intel.txt
    
    - name: Upload Intel artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ArgoLogViewer-macOS-Intel-v${{ github.event.inputs.version }}
        path: |
          ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.dmg
          ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.zip
          checksums-intel.txt
        retention-days: 90

  build-macos-arm:
    # Build Apple Silicon (arm64) binary
    runs-on: macos-latest  # Apple Silicon runner
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        echo "📦 Installing pip packages for Apple Silicon build..."
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        echo "✅ Dependencies installed"
    
    - name: Create macOS icon (.icns)
      run: |
        echo "🎨 Creating .icns icon for macOS..."
        mkdir -p app/icon.iconset
        sips -z 16 16 app/ICON.png --out app/icon.iconset/icon_16x16.png
        sips -z 32 32 app/ICON.png --out app/icon.iconset/icon_16x16@2x.png
        sips -z 32 32 app/ICON.png --out app/icon.iconset/icon_32x32.png
        sips -z 64 64 app/ICON.png --out app/icon.iconset/icon_32x32@2x.png
        sips -z 128 128 app/ICON.png --out app/icon.iconset/icon_128x128.png
        sips -z 256 256 app/ICON.png --out app/icon.iconset/icon_128x128@2x.png
        sips -z 256 256 app/ICON.png --out app/icon.iconset/icon_256x256.png
        sips -z 512 512 app/ICON.png --out app/icon.iconset/icon_256x256@2x.png
        sips -z 512 512 app/ICON.png --out app/icon.iconset/icon_512x512.png
        sips -z 1024 1024 app/ICON.png --out app/icon.iconset/icon_512x512@2x.png
        iconutil -c icns app/icon.iconset -o app/icon.icns
        rm -rf app/icon.iconset
        echo "✅ Icon created: app/icon.icns"
    
    - name: Build macOS Apple Silicon app
      run: |
        export MACOSX_DEPLOYMENT_TARGET=11.0
        
        echo "🔨 Building Apple Silicon (arm64) app with PyInstaller..."
        echo "📋 Deployment target: macOS 11.0 Big Sur and later (including Sonoma)"
        python -m PyInstaller --name="ArgoLogViewer" \
          --windowed \
          --onefile \
          --icon="app/icon.icns" \
          --add-data="app:app" \
          --hidden-import=PySide6 \
          --hidden-import=paramiko \
          --hidden-import=cryptography \
          --clean \
          --osx-bundle-identifier=com.harshmeetsingh.argologviewer \
          --target-arch arm64 \
          --codesign-identity - \
          app/main.py
    
    - name: Verify Apple Silicon binary
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🔍 VERIFYING APPLE SILICON BINARY"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        
        EXECUTABLE="dist/ArgoLogViewer.app/Contents/MacOS/ArgoLogViewer"
        
        if [ ! -f "$EXECUTABLE" ]; then
          echo "❌ ERROR: Executable not found at $EXECUTABLE"
          exit 1
        fi
        
        echo "📦 Checking architecture in executable:"
        lipo -info "$EXECUTABLE"
        file "$EXECUTABLE"
        echo ""
        
        if lipo -info "$EXECUTABLE" | grep -q "arm64"; then
          echo "✅ SUCCESS: Binary is Apple Silicon arm64!"
          echo "   ✓ Native Apple Silicon support (M1, M2, M3, M4)"
          echo "   ✓ Compatible with macOS 11.0+ (Big Sur, Monterey, Ventura, Sonoma)"
          echo "   ✓ Maximum performance on Apple Silicon Macs"
        else
          echo "❌ ERROR: Binary is not arm64!"
          exit 1
        fi
        
        echo ""
        echo "📋 Executable info:"
        ls -lh "$EXECUTABLE"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    - name: Create Apple Silicon DMG installer
      run: |
        mkdir -p dist/dmg
        cp -r dist/ArgoLogViewer.app dist/dmg/
        ln -s /Applications dist/dmg/Applications
        hdiutil create -volname "Argo Log Viewer (Apple Silicon)" \
          -srcfolder dist/dmg \
          -ov -format UDZO \
          "ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.dmg"
        rm -rf dist/dmg
    
    - name: Zip the Apple Silicon .app
      run: |
        cd dist
        zip -r ../ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.zip ArgoLogViewer.app
        cd ..
    
    - name: Generate Apple Silicon checksums
      run: |
        shasum -a 256 ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.dmg > checksums-arm.txt
        shasum -a 256 ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.zip >> checksums-arm.txt
        cat checksums-arm.txt
    
    - name: Upload Apple Silicon artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ArgoLogViewer-macOS-AppleSilicon-v${{ github.event.inputs.version }}
        path: |
          ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.dmg
          ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.zip
          checksums-arm.txt
        retention-days: 90

  create-release:
    needs: [build-macos-intel, build-macos-arm]
    runs-on: ubuntu-latest
    if: github.event.inputs.create_release == 'true'
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
    
    - name: Combine checksums
      run: |
        cat ArgoLogViewer-macOS-Intel-v${{ github.event.inputs.version }}/checksums-intel.txt > CHECKSUMS.txt
        cat ArgoLogViewer-macOS-AppleSilicon-v${{ github.event.inputs.version }}/checksums-arm.txt >> CHECKSUMS.txt
        echo "=== CHECKSUMS ==="
        cat CHECKSUMS.txt
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.event.inputs.version }}
        name: Argo Log Viewer v${{ github.event.inputs.version }} (macOS)
        body: |
          ## 🍎 macOS Release v${{ github.event.inputs.version }}
          
          ### 📥 Downloads
          
          **Choose the correct version for your Mac:**
          
          | Mac Type | Download (DMG) | Download (ZIP) |
          |----------|----------------|----------------|
          | 💻 **Intel Macs** | [macOS-Intel.dmg](../../releases/download/v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.dmg) | [macOS-Intel.zip](../../releases/download/v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.zip) |
          | 🚀 **Apple Silicon (M1/M2/M3/M4)** | [macOS-AppleSilicon.dmg](../../releases/download/v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.dmg) | [macOS-AppleSilicon.zip](../../releases/download/v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.zip) |
          
          **Not sure which Mac you have?**
          - Click the Apple menu () → "About This Mac"
          - **Intel Macs:** Shows "Processor: Intel Core i5/i7/i9"
          - **Apple Silicon:** Shows "Chip: Apple M1/M2/M3/M4"
          
          ### ✅ Compatibility
          
          - **macOS 11.0 Big Sur** and later
          - **macOS 12.0 Monterey** ✅
          - **macOS 13.0 Ventura** ✅
          - **macOS 14.0 Sonoma** ✅
          - **macOS 15.0 Sequoia** ✅
          
          ### 📋 Installation
          
          **DMG Method (Recommended):**
          1. Download the correct DMG file for your Mac type
          2. Open the DMG file
          3. Drag "ArgoLogViewer" to the Applications folder
          4. Eject the DMG
          5. Right-click the app → "Open" (first time only, to bypass Gatekeeper)
          
          **ZIP Method:**
          1. Download and unzip the correct ZIP file
          2. Move ArgoLogViewer.app to Applications folder
          3. Run in Terminal: `xattr -cr /Applications/ArgoLogViewer.app`
          4. Right-click the app → "Open" (first time only)
          
          ### ✅ Checksums (SHA-256)
          
          See `CHECKSUMS.txt` for SHA-256 verification of downloads.
          
          ### 🐛 Issues?
          
          Report at: https://github.com/${{ github.repository }}/issues
        draft: false
        prerelease: false
        files: |
          ArgoLogViewer-macOS-Intel-v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.dmg
          ArgoLogViewer-macOS-Intel-v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-Intel.zip
          ArgoLogViewer-macOS-AppleSilicon-v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.dmg
          ArgoLogViewer-macOS-AppleSilicon-v${{ github.event.inputs.version }}/ArgoLogViewer-v${{ github.event.inputs.version }}-macOS-AppleSilicon.zip
          CHECKSUMS.txt
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
