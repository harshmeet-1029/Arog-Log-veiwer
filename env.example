# Argo Log Viewer - Environment Configuration Example
# Copy this file to .env and customize for your environment

# =============================================================================
# SSH Connection Configuration
# =============================================================================

# Jump host (bastion) hostname or alias from ~/.ssh/config
# Default: usejump
ARGO_JUMP_HOST=usejump

# Internal Kubernetes server IP or hostname
# Default: 10.0.34.231
ARGO_INTERNAL_HOST=10.0.34.231

# Service account username for kubectl operations
# Default: solutions01-prod-us-east-1-eks
ARGO_SERVICE_ACCOUNT=solutions01-prod-us-east-1-eks

# Path to SSH config file
# Default: ~/.ssh/config
# SSH_CONFIG_PATH=~/.ssh/config

# =============================================================================
# Kubernetes Configuration
# =============================================================================

# Kubernetes namespace for pod operations
# Default: argo
ARGO_NAMESPACE=argo

# =============================================================================
# Security Configuration
# =============================================================================

# Enable log sanitization to prevent log injection attacks
# Values: true, false, 1, 0, yes, no
# Default: true
ARGO_LOG_SANITIZATION=true

# Enable strict SSH host key checking (recommended for production)
# When enabled, connection will fail if host key is not in known_hosts
# When disabled, warnings are shown but connection proceeds
# Values: true, false, 1, 0, yes, no
# Default: false
# IMPORTANT: Enable this in production after properly configuring known_hosts
ARGO_STRICT_HOST_KEY_CHECKING=false

# =============================================================================
# Notes
# =============================================================================
#
# 1. SSH Authentication:
#    - This application uses SSH key-based authentication
#    - Configure your SSH keys in ~/.ssh/config
#    - No passwords are required or stored
#
# 2. Security:
#    - Enable ARGO_STRICT_HOST_KEY_CHECKING=true in production
#    - Use strong SSH keys (RSA 4096-bit or Ed25519)
#    - Protect your private keys with passphrases
#
# 3. Permissions:
#    - This file may contain sensitive information
#    - Ensure proper file permissions: chmod 600 .env
#    - Never commit .env to version control
#
# =============================================================================
